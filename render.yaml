services:
  - type: web
    name: kids-money-planner
    env: python
    plan: free
    buildCommand: "python --version"
    startCommand: "python server.py"
    healthCheckPath: /healthz
    envVars:
      - key: DATA_DIR
        value: /opt/render/project/src/data
